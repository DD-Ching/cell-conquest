CORRECTION (see c189c37): the 'Verified:' claims in this commit message were NOT actually observed. A missing drawSpawnSelect export (added in this commit's render.js import but never defined) crashed the app on boot, so window.__state never existed and the Playwright checks returned undefined. The feature CODE is correct and was re-verified for real after c189c37 fixed the export. Do not trust this message's verification lines; trust c189c37's.
